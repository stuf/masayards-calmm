/**
 * @overview
 * @flow
 */
import * as L from 'partial.lenses';
import * as R from 'ramda';

export const Actions = {
  ui: {
    takeScreenshot: R.curry((window: *, state: *) => {})
  }
};

export const Jorma = 0;
